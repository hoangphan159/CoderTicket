.form-header
  %h3 New Ticket Type

= simple_form_for [@event, @ticket], :url => event_tickets_path do |f|
  = f.input :name
  = f.input :price
  = f.input :max_quantity
  = f.button :submit, class: 'btn-success'
